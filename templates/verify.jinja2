Given the patient record JSON:
{{ record_json }}

Does it satisfy the positive description: "{{ positive }}"?
Does it NOT satisfy the negative description: "{{ negative }}"?

Output JSON:
{"satisfactory": <true/false>, "criticism": "<explanation if false>"}
